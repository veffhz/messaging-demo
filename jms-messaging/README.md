# Spring demo project (jms, activeMQ)

#### requirements:
java 8, maven.

#### build
`mvn clean package`

#### run
`mvn spring-boot:run -pl jms-messaging -am` or `java -jar target/jms-messaging.jar`

### documentation

Domain model:
* Message

